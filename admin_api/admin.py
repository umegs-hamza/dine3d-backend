from django.contrib import admin

# Nothing registered here — these models already have dedicated admin.py files
# in their own apps (accounts, restaurants, menu). This app only exposes the
# /api/v1/admin/ REST endpoints for the React admin dashboard.
